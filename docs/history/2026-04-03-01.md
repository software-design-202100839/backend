# 세션 히스토리 2026-04-03-01

- 페르소나: 이데브 (DevOps Lee), 이백엔드 (Backend Lee), 이프론트 (Frontend Lee)
- Sprint: 3

## 완료

### SSCM-55 Jira 완료 전환 (지난 세션 미완료)
- Jira SSCM-55 상태: 진행 중 → 완료 (transition id: 31)

### SSCM-56: CD 완성 + 프로덕션 배포
- **CD 워크플로우 확장** (Backend PR #5, Frontend PR #3)
  - ECR push 후 `aws ecs update-service --force-new-deployment`
  - `aws ecs wait services-stable` 배포 안정성 대기
  - 배포 후 태스크 상태 검증 단계
- **GitHub Secrets 등록**: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY (양쪽 레포)
- **ECR 리포지토리 생성**: sscm-backend, sscm-frontend (AWS 콘솔)
- **CloudFormation 스택 배포**
  - `sscm-alb`: ALB + Target Group + Listener Rule + Parameter Store
  - `sscm-data`: RDS PostgreSQL db.t3.micro + ElastiCache Redis cache.t3.micro (신규 템플릿)
  - `sscm-ecs`: ECS Fargate 클러스터 + 서비스 (수정된 템플릿)
- **프로덕션 배포 성공**: ALB URL로 전 기능 접속 확인

### 버그 수정
1. **actuator 헬스체크 인증 차단** (Backend PR #8)
   - SecurityConfig에 `/actuator/**` permitAll 추가
2. **학생 회원가입 admissionYear NPE** (Backend PR #7, Frontend PR #5)
   - 백엔드: null이면 현재 연도 기본값
   - 프론트엔드: 입학년도 필드 추가
3. **API baseURL 하드코딩** (Frontend PR #4)
   - `VITE_API_BASE_URL` 환경변수 분리 (프로덕션: `/api/v1`)
4. **Playwright 프로덕션 대응** (Frontend PR #5)
   - `BASE_URL` 환경변수로 baseURL/webServer 분기

### 인프라 수정
- `cfn-ecs-cluster.yml`: AssignPublicIp DISABLED → ENABLED (Backend PR #6)
- `cfn-data.yml` 신규 생성: RDS + Redis CloudFormation (Backend PR #6)
- `application-prod.yml`: MAIL_USERNAME/PASSWORD 기본값 추가 (Backend PR #6)
- ECS 서비스: HealthCheckGracePeriodSeconds 0 → 120 (콘솔에서 수정)

### 프로덕션 검증
- API 테스트: 회원가입(교사/학생/학부모), 로그인, 성적 입력/조회, 피드백, 상담
- Playwright E2E: 프로덕션 URL 대상 7/7 통과 (32.8s)

## 트러블슈팅
- 10건 기록: `docs/troubleshooting/SSCM-56-production-deploy.md`

## 미완료
- Jira SSCM-56 → 완료 전환
- cfn-ecs-cluster.yml DesiredCount/HealthCheckGracePeriod 반영 커밋
- AWS 스택 정리 (비용 절약)

## 발견한 개선 사항
- 학생 로그인 시 UX: 현재 교사 관점 UI → 학생은 자기 정보만 바로 보여야 함

## 남은 작업 (스프린트 3)
| Jira | 일정 | 내용 |
|------|------|------|
| SSCM-57 | 4/4 | Burp Suite DAST |
| SSCM-58 | - | @Version + @Retryable (nice-to-have) |
