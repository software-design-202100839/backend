# 세션 히스토리 2026-04-09-01

- 페르소나: 이데브 (DevOps Lee) + 이큐에이 (QA Lee)
- Sprint: 4

## 완료

### 1. AWS 인프라 전체 재구축
- 전체 스택이 삭제된 상태에서 처음부터 재구축
- AWS CLI v2 설치, sscm-admin IAM 유저 생성
- ECR 레포 생성 → Docker 이미지 빌드/push → 3개 스택 순차 생성
- sscm-data (RDS + Redis), sscm-alb (ALB + Parameter Store), sscm-ecs (ECS Fargate)
- Encryption Key Base64 인코딩 이슈 해결 (Illegal base64 character 2d)
- ALB DNS: `sscm-alb-1703346258.ap-northeast-2.elb.amazonaws.com`

### 2. Prometheus + Grafana 모니터링 (PR #14)
- micrometer-registry-prometheus 의존성 추가
- Actuator prometheus 엔드포인트 노출 (prod/dev)
- docker-compose에 Prometheus + Grafana 서비스 추가 (로컬)
- cfn-monitoring.yml: ECS Fargate 모니터링 스택 (Prometheus + Grafana 단일 Task)
- SSCM Overview 대시보드 10개 패널 (JVM, HTTP, HikariCP, GC)
- Grafana `/grafana/*` 서브패스 ALB 라우팅

### 3. Grafana Alerting — PagerDuty 대체 (PR #15)
- PagerDuty는 Grafana Alerting과 기능 중복 → 불필요한 외부 의존성 추가 대신 기존 도구 활용
- Gmail SMTP 설정 + 이메일 알림 Contact Point (202100839@inu.ac.kr)
- Alert Rule 3개: HTTP 5xx 에러, JVM Heap 90% 초과, HikariCP 커넥션 풀 고갈

### 4. k6 부하 테스트 + 결과 분석 (PR #16)
- k6 테스트 시나리오: 로그인 → 8개 API 순차 호출 (10→50 VU, 3분)
- 병목 발견: 로그인 API (bcrypt + 512 CPU → 평균 10.9초)
- 판단: 동시 50명 로그인은 비현실적, 현 구성 유지. 필요 시 CPU 스케일업으로 대응
- Redis @Cacheable: 조회 API 이미 충분히 빠름 → 도입 불필요 판단

### 5. 백업/복구 절차 (PR #17)
- RDS 자동 백업 활성화 (BackupRetentionPeriod: 0 → 7일)
- 백업/복구 절차 문서: 자동 백업, 수동 스냅샷, PITR, Flyway 롤백
- Redis 백업 불필요 판단 (JWT 토큰 캐시 전용, TTL 자동 만료)

### 6. SonarCloud 연동 (PR #18)
- SonarCloud 프로젝트 연동 (organization: software-design-202100839)
- CI 워크플로우: develop push/PR → 테스트 + JaCoCo + SonarCloud 분석
- Automatic Analysis 비활성화 (CI 분석과 충돌 방지)

## 스킵/변경 판단

| 태스크 | 판단 | 이유 |
|--------|------|------|
| Dynatrace APM | 스킵 | Prometheus+Grafana와 기능 중복. 설계적 근거 없는 도구 추가는 부적절 |
| PagerDuty | Grafana Alerting 대체 | 별도 SaaS 불필요. 이미 구축된 Grafana로 충분 |
| Redis @Cacheable | 불필요 판단 | 조회 API 이미 빠름. 캐시 무효화 복잡도 대비 이점 미미 |
| SSCM-58 @Version+@Retryable | 후순위 | 백업/복구, SonarCloud, 문서 정리가 마감 기한상 더 급함 |

## 멈출 때 상태
- develop 브랜치 최신 상태
- AWS 4개 스택 가동 중 (sscm-data, sscm-alb, sscm-ecs, sscm-monitoring)
- 남은 태스크: 전체 문서 정리 (4/10~11), SSCM-58 (시간 여유 시)
