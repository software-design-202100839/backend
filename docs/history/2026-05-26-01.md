# 2026-05-26 작업 기록

## 목표
발표 준비: 테스트 코드 + AWS 배포 + 부하 테스트 수치 확보

---

## 완료 작업

### 1. Phase 6 테스트 코드 작성
- OLAP 분석 시스템 단위/컨트롤러/통합 테스트 31개 작성
- 기존 테스트 수정 (eventPublisher mock 추가)
- 전체 407 tests passed
- 커밋: `c328ca3` → develop push 완료

### 2. AWS 풀 아키텍처 배포
CloudFormation으로 전체 인프라 구축:

| 스택 | 리소스 | 상태 |
|------|--------|------|
| sscm-alb | ALB + SSM Parameter Store (13개) | 생성 완료 |
| sscm-data | RDS 운영 + RDS 분석 + ElastiCache Redis | 생성 완료 |
| sscm-msk | MSK Kafka (kafka.t3.small x2) | 생성 완료 |
| sscm-ecs | ECS Cluster + Backend/Frontend 서비스 | 생성 완료 |

ALB DNS: `sscm-alb-942993728.ap-northeast-2.elb.amazonaws.com`

### 3. Redis 도입 (JWT 블랙리스트 캐시)
- `TokenBlacklistService`: Redis 1차 조회 + DB fallback 패턴
- `spring-boot-starter-data-redis` 의존성 추가
- `docker-compose.yml`에 Redis 7 Alpine 추가
- 테스트 6개 작성 (Redis hit/miss/fallback)

### 4. 대량 테스트 데이터 생성
- `DevSeedController`에 `/seed/bulk` 엔드포인트 추가
- 학생 30명 + 과목 5개 + 성적 150건 + 피드백 60건 + 상담 30건
- Kafka 이벤트 발행 → Consumer 집계 → 분석 DB 적재

### 5. 부하 테스트 (k6)
3회 실행:

| 구분 | p50 | p95 | 처리량 |
|------|-----|-----|--------|
| 로컬 (참고) | 6.49ms | 9.84ms | 222 req/s |
| AWS Before (Redis 없음, 데이터 없음) | 303.7ms | 906.2ms | 133 req/s |
| AWS After (Redis, 데이터 없음) | 450.9ms | 1,090ms | 116 req/s |
| AWS 최종 (Redis + 데이터 30명) | 297.4ms | **791.2ms** | **140 req/s** |

Before → 최종: p95 **13% 개선**, 처리량 **5% 향상**

---

## 발견된 이슈 및 해결

| 이슈 | 원인 | 해결 |
|------|------|------|
| ECS Task 시작 실패 | SSM db-password가 CHANGE_ME | ssm put-parameter로 업데이트 |
| /dev/seed 404 | prod 프로파일에서 @Profile("dev") | @Profile({"dev", "prod"})로 변경 |
| dev.seed.admin.password 미해결 | application-prod.yml에 누락 | dev.seed 섹션 추가 |
| /seed/bulk 500 (phone 중복) | 기존 user와 전화번호 충돌 | 기존 유저 재사용 로직 |
| /seed/bulk 500 (student_num 중복) | enrollment student_num 충돌 | 오프셋 10+i 적용 |
| Docker 캐시 미반영 | COPY src src 캐시됨 | --no-cache 빌드 |
| admin 로그인 실패 | bulk seed가 admin 계정 영향 | seed/all 재실행 |

---

## 변경된 파일

### 새로 생성
- `src/test/java/com/sscm/analytics/` — 테스트 7개 파일
- `docs/spec/olap/phase6-testing.md`
- `docs/spec/olap/step2-loadtest-guide.md`
- `docs/spec/olap/step2-before-results.md`
- `docs/spec/olap/step3-redis-integration.md`
- `docs/spec/olap/step4-after-results.md`
- `docs/spec/olap/aws-deployment-guide.md`
- `docs/sprint/final-presentation-plan.md`
- `infra/loadtest/k6-dashboard-api.js`
- `infra/cfn-msk.yml`

### 수정
- `build.gradle.kts` — Redis 의존성
- `TokenBlacklistService.java` — Redis 캐시
- `TokenBlacklistServiceTest.java` — Redis mock
- `application-prod.yml` — Redis/Kafka/Analytics 설정
- `application-dev.yml` — Redis 설정
- `application-test.yml` — Redis 설정
- `docker-compose.yml` — Redis 컨테이너
- `DevSeedController.java` — bulk seed + prod 프로파일
- `cfn-data.yml` — Analytics RDS 추가
- `cfn-alb.yml` — SSM 파라미터 추가
- `cfn-ecs-cluster.yml` — 환경변수 추가
- `CounselingServiceTest.java` — eventPublisher mock
- `StudentRecordServiceTest.java` — eventPublisher mock

---

## 남은 작업

| # | 작업 | 상태 |
|---|------|------|
| 1 | CD 워크플로우 → ECS 배포 수정 | ⬜ |
| 2 | 병목 개선 (Analytics DB 쿼리 최적화) | ⬜ |
| 3 | WebSocket Redis Pub/Sub | ⬜ |
| 4 | S3 + CloudFront (Frontend) | ⬜ |
| 5 | Monitoring 배포 (Prometheus + Grafana) | ⬜ |
| 6 | 아키텍처 다이어그램 (draw.io) | ⬜ |
| 7 | 발표 자료 정리 | ⬜ |

---

## AWS 비용 참고
- 전체 인프라 가동 시간: ~2시간
- 예상 비용: ~$0.60 (약 800원)
- MSK가 비용의 ~50% 차지
