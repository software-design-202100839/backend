spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sscm
    username: sscm
    password: sscm1234  # NOSONAR — dev/local only, not a production credential
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

encryption:
  key: c3NjbS1kZXYtZW5jcnlwdGlvbi1rZXktMzItYnl0ZXM=

jwt:
  secret: sscm-dev-jwt-secret-key-must-be-at-least-256-bits-long-for-hs256
  access-token-expiration: 1800000
  refresh-token-expiration: 604800000

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus,info,metrics

logging:
  level:
    com.sscm: DEBUG
    org.springframework.security: DEBUG
